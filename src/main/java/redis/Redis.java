package redis;

import org.springframework.boot.autoconfigure.data.redis.RedisProperties;

import java.net.URI;
import java.net.URISyntaxException;

public class Redis {

//    private static RedisProperties.Jedis getConnection() throws URISyntaxException {
//        URI redisURI = new URI(System.getenv("REDIS_URL"));
//        RedisProperties.Jedis jedis = new RedisProperties.Jedis(redisURI);
//        return jedis;
//    }
//
}
